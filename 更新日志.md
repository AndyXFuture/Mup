# 更新日志

本项目每次修改都会及时更新此文件。

## v0.3.2（2026-08-30）

**修复：右键托盘「退出」无效（不停止播放、不关闭应用）**。

- 根因 1：托盘右键 `QMenu()` 为局部变量、无 parent，Python GC 可能回收导致「退出」菜单项信号失效
- 根因 2：`_quit_app` 只 `close()` 主窗口，依赖 `quitOnLastWindowClosed`，托盘场景下不保证退出事件循环
- 修复：持有 `self._tray_menu` 引用；`_quit_app` 依次 **停止播放（player.stop）→ 关闭窗口 → 显式 `QApplication.quit()`**，确保进程真正结束
- 验证：真实平台冒烟 EXIT=0；`_quit_app` 调用链 stop→close→quit 断言通过

---

## v0.3.1（2026-08-30）

**沉浸页/标题栏修复 + 图标修正 + 双击标题栏最大化 + 打包 exe**。

- 修复：封面放大动画**放大后的位置偏下**（根因：v0.3.0 进入沉浸页隐藏标题栏，导致动画目标矩形按有标题栏计算、结束后页面回移 38px）；改为**恢复标题栏常显**，目标位置与最终位置一致
- 修复：沉浸页返回键**位置与点击区域错位**（标题栏隐藏后返回键落入 WM_NCHITTEST 的 HTCAPTION 拖动区）；右上角**设置/最小化/最大化/关闭消失**——随标题栏恢复一并解决
- **双击标题栏切换窗口/最大化**：WM_NCHITTEST 对标题栏返回 HTCAPTION（Windows 系统级双击最大化/还原行为）
- 修复：**音量/静音图标深色模式下渲染为黑色**（`currentColor` 不被 QtSvg 解析 → 改用 `@FILL@` 占位并在渲染时注入颜色；`repeat_one` 圆点同步修复），浅/深色均按主题色渲染
- 上/下一曲图标：三角形**加圆角**（RoundJoin 描边）、**尖角与竖条贴合**更紧凑（整体宽度约 0.9×图标）
- 沉浸页内容区上边距 20→12（上=左，返回键更靠上）

---

## v0.3.0（2026-08-30）

**托盘常驻 + 单文件打包 + 歌单自定义封面 + 沉浸页/设置打磨**（三轮需求）。

### 播放栏 / 沉浸页
- 沉浸页返回键上移：进入时隐藏整个标题栏，返回键真正贴窗口左上角（上=左=20）；退出恢复标题栏
- 播放栏图标改 **QQ 音乐圆润可爱风**：音量=填充小喇叭+圆头声波、歌词=粗圆头三线、循环/随机/单曲/刷新=粗 2.2 圆角
- 修复：调音量不显示百分比 → 改为系统 QToolTip（跟随鼠标、跨窗口不裁切），两页播放栏一致

### 设置界面
- 重写为**分组卡片式**：圆角卡片（连接设置/外观主题/快捷键/关于）、主题色节标题、快捷键用代码样式圆角 chip + Consolas 字体、地址可复制

### 系统
- **关闭最小化到托盘**：点 × 隐藏到托盘（保留播放），双击/菜单恢复，右键「退出」才真正退出；托盘不可用时关闭直接退出
- **单文件打包**：`config.py` 兼容 PyInstaller（配置/logs 写 exe 目录、资源走 `_MEIPASS`）；新增「使用方法.txt」含一行打包指令
- **记录上次窗口尺寸**：启动恢复、关闭保存（`player.window_size`）

### 歌单
- **自定义封面**：头部刷新按钮右侧新增编辑（铅笔）图标 → 菜单选「选择本地图片… / 使用服务器封面 / 使用自定义封面」；图片复制到 `covers/` 持久化，侧边栏行与头部大封面均按自定义优先显示（解决音桥不同步飞牛自定义歌单封面）
- **歌单两段加载**：先快速取前 50 首立即可用，后台再拉全量（Jellyfin 补全）后无缝替换并写缓存

---

## v0.2.0（2026-08-30）

**完整歌单（Jellyfin 协议补全）+ 沉浸页重做 + 重启恢复 + 交互打磨**（四轮需求，真实服务器联调验证）。

### 完整歌单加载
- 音桥 Subsonic `getPlaylist` 固定只返回前 50 首（分页参数被忽略）→ 新增 **Jellyfin 协议**分页补全：`Playlists/{id}/Items` 取全量并映射回 Subsonic Song（剥离 `fnm-track-/album-/artist-` 前缀），封面走 Jellyfin 公开图片接口；「我喜欢」2023 首完整加载（约 1.4s）
- 侧边栏歌单显示真实歌曲数（getPlaylist songCount，暗色、无单位）

### 播放栏
- 上/下一曲改为**实心三角形 + 圆角竖条**（新增 `SkipButton`，方向修正：上一曲尖朝左、下一曲尖朝右）
- 音量：点击图标**静音/恢复记忆音量**（新增 `mute` 图标 + `volume_changed` 信号）；滑块加长一倍（120px，悬浮 150）并在调整/悬浮时显示**实时百分比**
- 除播放/暂停外其它图标加大（19px）；歌单页与沉浸页播放栏统一（沉浸页左下不显示歌曲信息、爱心移到播放顺序键左侧）
- 播放中封面四柱律动动画改**白色**、居中、放慢

### 沉浸页
- 布局改为**左右 50%**：封面居左、歌词居右；封面随窗口自适应（`min(高度×0.42, 宽度×0.32, 360)`）
- 打开时封面由过渡层放大，**其它内容渐变出现**（`fade_in`，顶栏/歌词/底部渐显）
- 修复：**首次点击左下角封面过渡放得特别大**（沉浸页未布局时 big_cover 为默认 640×480，打开前先按内容区强制布局）

### 歌单页 / 列表
- 列布局：封面 56→48、歌名/歌手紧凑间距 5、专辑列 120→220、表头与封面/内容严格左对齐、时长左对齐
- 封面悬浮图标改为实心三角/暂停；行悬浮（未悬图标）白色、悬停图标主题色
- **歌手名/专辑名可点击跳转**（`artistId` 解析、悬浮高亮+下划线、`open_artist`/`open_album`、播放栏歌手可点）；歌手名点击范围收窄到文本实际宽度
- 歌单加载：**缓存 + 立即「加载中」状态 + 随机播放旁刷新按钮**（本次已加载的不重载，可手动强制刷新）
- 专辑网格**流体自适应**列数/格子尺寸，消除右侧空隙

### 侧边栏
- 歌单与上方导航**唯一选中互斥**（选歌单清导航、选导航清歌单）；歌单行悬浮(press)/选中(primary_soft + 左侧指示条)明显区分
- 去掉搜索框与「音乐库」分栏；「我的歌单」居中；首页与收藏之间去掉额外间距

### 其他
- **重启恢复**：默认打开上次歌单并显示上次歌曲（`Player.restore`，不自动播放、恢复进度），音量/播放模式一并恢复
- 迷你歌词背景改**底部半透明→顶部全透明**（向上渐隐）；侧边栏图层高于歌词层
- 歌手页跳转：左侧歌手列表自动填充并**滚动定位 + 选中**目标歌手
- 修复：`SortHeader` 使用未定义 `_q`、`windows.py` 过渡层 `QRectF` 未导入两个崩溃

---

## v0.1.5（2026-08-30）

**UI 全面打磨（8 项）**，沉浸页布局重做，暗色模式改为 QQ 音乐深灰黑背景（主题色保持紫色）。

- 迷你歌词字号 9→12.5pt、高度 40→46、更亮
- 播放栏爱心移到歌名同一行（歌手与爱心左对齐）；点亮时变实心爱心（新增 heart_filled 图标）
- 统一三页与窗口框架的外边距（16px 体系）；暗色背景改为近黑炭 `#1a1a1a` 系（参考 QQ 音乐），主题色保持紫色
- 封面放大过渡动画改为圆角裁剪（半径在源/目标间插值），不再短暂变直角
- 沉浸页按参考图重做：封面左侧垂直居中、歌名/歌手/歌词在右上栏、歌词左对齐当前行居中、底部进度+控制栏
- 沉浸页返回按钮左边距=上边距（20/20）
- 输入框文本左侧留白（textMargins 14）；侧边栏"我的歌单"显示真实封面（缺失时用第一首歌封面兜底），并随封面加载刷新
- 进度条悬浮时左侧时间显示鼠标位置对应时间（HoverSlider 新增 hoverValueChanged/hoverEnded）
- 修复：侧边栏歌单封面加载后未重绘；歌词改用左对齐+距离中心渐隐

---

## v0.1.4（2026-08-30）

**崩溃热修复**：启动后鼠标悬浮/点击歌曲列表封面时报 `QListView.indexAt` 参数类型错误。

- 根因：事件过滤器把 `event.position()`（`QPointF`）直接传给 `QListView.indexAt()`（只接受 `QPoint`）
- 修复：`_row_at()` 统一做 `QPointF→QPoint` 转换；点击封面传正确坐标；`eventFilter` 加异常保护避免任何事件异常再打崩程序
- 验证：模拟悬浮（hover_row/cover_hover 正常）与点击封面（1.5s 后进入播放，position=1437ms）均通过

---

## v0.1.3（2026-08-30）

**交互与细节完善**（14 项需求逐项落地，真实服务器联调验证）。

### 列表与排序
- 歌曲列表封面加大至 44px；悬浮变暗 + 实心三角（悬浮封面变主题色），点击开始播放变为暂停标志，移开鼠标后当前播放行封面显示**四柱律动动画**（播放时定时刷新）
- 列头可点击循环切换排序：歌名/歌手（默认→歌名↑↓→歌手↑↓）、专辑（默认→↑↓）、时长（默认→↑↓）；歌名/歌手列宽加大至 270，压缩专辑/时长
- 歌手页改为**双栏**：左侧窄列（与侧边栏等宽，236px）按**歌曲数降序**（快速聚合专辑列表秒级完成，不再逐歌手请求），右侧显示该歌手歌曲列表
- 移除「音乐库」分组里的「歌单」导航（歌单只在「我的歌单」栏）；「设置」移到标题栏最小化按钮左侧

### 播放栏
- 爱心移到歌名前；播放键真居中（左右等宽拉伸）；歌名/歌手超宽时**跑马灯前后滚动**
- 播放栏上方新增**迷你歌词条**：显示当前歌词，背景由不透明渐变到半透明（陶土底）

### 沉浸页与过渡
- 沉浸页布局修正：封面放左侧、标题/歌手放封面下方、歌词垂直居中
- 列表→沉浸过渡改为**放大到沉浸页新封面位置**（歌词上方），不再全屏

### 系统与快捷键
- **媒体键改用低级键盘钩子（WH_KEYBOARD_LL）** 全局捕获播放/暂停、上/下一曲、音量±，不再依赖窗口焦点；另加 WM_APPCOMMAND 兜底（此前 RegisterHotKey 方式因被其他应用占用而无效）
- 主题默认**跟随系统**（设置提供 跟随系统/浅色/深色，系统切换 15 秒内自动跟随）
- 新增应用图标 `resources/app_icon.ico`（设置到应用与窗口）

### 修复
- 修复 `_q()` 未导入、`QColor.with_alpha_value` 不存在导致的两个 paintEvent 崩溃
- 歌单加载改为分页感知（服务器支持则取完，音桥固定 50 首则自动停止）

---

## v0.1.2（2026-08-30）

**真实联调修复 + 功能补齐**：连接真实音桥服务器后发现解析器与音桥 JSON 不兼容，导致歌单/歌曲/封面全空。

### 修复（关键）
- **解析器兼容音桥 JSON**：音桥返回原生 JSON（纯键 `id`/`name`/`title`），标准 Subsonic XML→JSON 用 `@` 前缀键。新增 `_get()` 统一兼容两种键风格，`_parse_song/_parse_playlist/_parse_album/_parse_artist` 全部修正
- 歌词改用 OpenSubsonic `getLyricsBySongId`（结构化时间轴歌词），无则回退 `getLyrics`

### 新增功能（导航占位全部实现）
- **首页**：最近专辑网格（getAlbumList2）
- **专辑**：专辑网格（真实封面+名称+歌曲数），点击进入专辑歌曲列表
- **歌手**：歌手列表（1506 位），点击歌手 → 该歌手专辑网格 → 点击专辑 → 歌曲
- **搜索**：侧边栏搜索框，search3 搜歌曲/专辑/歌手
- **最近播放**：getPlayQueue（音桥当前为空时显示"暂无记录"）
- 通用歌曲头（大封面+标题+统计+播放全部/随机播放）复用于歌单/专辑/搜索/收藏

### 验证
- 用真实服务器端到端验证：歌单 50 首、歌词 58 行带时间轴、1506 位歌手、搜索、QMediaPlayer 流播放（时长 309s、进度正常）
- 三个视图真实数据渲染截图确认（歌单/专辑/歌手）

---

## v0.1.1（2026-08-30）

**UI 视觉完善**：对照预览效果修复实际渲染差距。

### 修复
- 修复 `QPainterPath` 未导入导致启动崩溃（access violation）的问题
- 修复歌曲行"专辑/歌手"重叠问题，改为规范三列布局：歌名/歌手（左）｜专辑（中）｜时长（右）

### 完善
- 主窗口绘制 **DESIGN.md 陶土底色** `#E8EAF0`，三个页面不再是透明白底
- 重写新拟态阴影系统：多层偏移阴影不再被控件边界裁剪，**深浅色自适应强度**（深色模式高光收敛，避免紫色光晕）
- 卡片/按钮预留内部阴影边距，阴影完整显示
- 全局字号上调（标题/正文/说明），更接近预览的排版层级
- 歌曲列表新增**列头**：歌名/歌手｜专辑｜时长（占位视图自动隐藏）
- 底部播放栏加高（78→88px），布局更舒展
- 歌词加载完成后按当前进度重新定位高亮行

### 截图
- 更新 `docs/screenshot_*.png`（三页浅色）并新增 `docs/screenshot_library_dark.png`（深色）

---

## v0.1.0（2026-08-30）

**首个可运行版本**，完成「音珏 - Mup」Windows 音乐播放器全部核心功能。

### 新增
- 三段式界面（参考 QQ 音乐）：**连接服务器页 / 歌单页 / 沉浸页**
- 视觉风格严格遵循 [DESIGN.md](DESIGN.md) 的 **Extruded Light 浅色新拟态**（陶土底、靛蓝主色、外凸/内凹双阴影），并支持**浅色/深色一键切换**
- Subsonic / OpenSubsonic 协议客户端（token+salt 认证），通过「音桥」接入飞牛音乐
- 首次启动填写 IP/端口/用户名/密码，连接成功自动保存到 `config.json`，重启后自动重连；设置中可重新填写
- 基础播放功能：播放/暂停、上一曲/下一曲、进度拖动、音量调节、收藏
- 播放模式（单按钮 + 弹出菜单）：随机播放 / 顺序播放 / 单曲循环 / 列表循环（默认列表循环）
- 沉浸页：点播放栏封面或歌词按钮 → 封面放大过渡动画进入；大封面（点击在圆形/方形间切换，悬浮变暗显示切换图标）+ 随播放滚动高亮的歌词（LRC 时间轴 / 纯文本自适应）
- 全局快捷键：系统媒体键（播放暂停、上/下一曲、停止、音量±）+ 自定义组合键（默认 Ctrl+Alt+P / ← / → / ↑ / ↓）
- 无边框主窗口 + 自定义标题栏 + 原生边缘缩放（WM_NCHITTEST）
- 性能优化：歌曲列表虚拟化渲染（万首不卡）、封面异步加载 + 内存 LRU 缓存、网络全部后台线程
- 工程化：单实例、Python/Qt 双层崩溃日志（`logs/mup.log`）、`.gitignore`

### 已知限制（规划中）
- 首页 / 搜索 / 最近播放 / 歌手 / 专辑 导航暂为占位提示，后续版本实现
- 自定义快捷键暂在 `config.json` 中修改，后续提供图形化编辑
- 封面暂用内存缓存，未落盘；歌词仅支持 getLyrics（内嵌 LRC/纯文本）
