# 项目细节描述

> 本文件用于让下一个 AI 能迅速理解并接手程序，会随代码更新持续维护。

## 一、项目简介

**音珏 - Mup** 是一个 Windows 端的轻量音乐播放器：通过 **Subsonic / OpenSubsonic 协议**连接「音桥」桥接出的**飞牛音乐**服务，提供歌单浏览、流媒体播放、歌词与沉浸页等能力。界面布局参考 QQ 音乐（左侧导航 + 主内容 + 底部播放栏三段式），视觉风格严格遵循 [DESIGN.md](DESIGN.md)（Extruded Light 浅色新拟态，靛蓝主色 `#4648d4`）。

## 二、技术栈

- Python 3.10+ / **PySide6 (Qt Widgets，非 QML)**，保证低内存占用
- 音频：`QMediaPlayer` + `QAudioOutput`（走系统解码器，支持 HTTP Range 拖动进度）
- 网络：`requests`（后台线程）+ `QNetworkAccessManager`（封面异步加载）
- 全局快捷键：Win32 `RegisterHotKey` + `QAbstractNativeEventFilter`（WM_HOTKEY）
- 无边框窗口：WM_NCHITTEST 原生边缘缩放 + 自定义标题栏
- 打包目标：PyInstaller（暂未配置 spec）

## 三、目录结构

```
Mup/
├─ main.py                       # 入口：日志、单实例、启动
├─ requirements.txt
├─ app/
│  ├─ config.py                  # config.json 读写（服务器/播放/主题/快捷键）
│  ├─ subsonic_client.py         # Subsonic/Jellyfin 客户端（含完整歌单补全）
│  ├─ cover_manager.py           # 封面异步加载 + 内存 LRU 缓存
│  ├─ player.py                  # 播放核心（队列 + 四种播放模式 + 启动恢复）
│  └─ hotkeys.py                 # 全局快捷键（系统媒体键 + 自定义组合键）
├─ ui/
│  ├─ theme.py                   # DESIGN.md 配色（浅/深）+ QSS
│  ├─ icons.py                   # 统一线性 SVG 图标库（stroke 风格；填充部分用 @FILL@ 占位注入主题色）
│  ├─ widgets.py                 # 新拟态控件：NeuFrame/NeuButton/NeuIconButton/
│   │                            #   PlayButton/SkipButton/HoverSlider/NeuLineEdit/CoverArt/
│   │                            #   SongModel/SongDelegate/SimpleModel/LyricsWidget
│  ├─ windows.py                 # 无边框主窗口、标题栏、过渡动画、设置对话框
│  └─ pages/
│     ├─ connect_page.py         # 连接服务器页
│     ├─ library_page.py         # 歌单页（侧边栏+虚拟化列表+播放栏）
│     └─ player_page.py          # 沉浸页（大封面+歌词+控制）
├─ DESIGN.md / QQ音乐布局参考.png
├─ 更新日志.md / 项目细节描述.md / README.md / 提示词记录.txt
└─ config.json（运行时生成，不入库）
```

## 四、关键设计说明

### 1. 主题系统（ui/theme.py）
- `LIGHT` / `DARK` 两套调色板，`CURRENT` 为全局当前色板，所有自定义控件在 **paint 时读取** `theme.CURRENT`，因此换肤只需改色板 + 触发重绘。
- 新拟态绘制集中在 `ui/widgets.py`：
  - `_paint_raised_shadow()` 外凸 = 6 层偏移暗/亮阴影（近似模糊）+ 底色；阴影强度随主题 `shadow_dark/shadow_light` 的基础透明度**自适应缩放**（浅色强、深色收敛，避免紫色光晕）。
  - `_paint_inset_shadow()` 内凹 = 底色 + 顶部内阴影 + 底部内高光（QPainterPath 裁剪实现）。
  - `NeuFrame` 默认 `margin=7`，把阴影画在控件内部留白区，避免被控件边界裁剪；页面布局的 contentsMargins 需 ≥ margin 以免内容压住阴影。
- 主窗口 `MainWindow.paintEvent` 负责整窗陶土底色（浅色 `#E8EAF0`），页面透明叠加其上。
- 暗色模式背景采用 **QQ 音乐深灰黑**（`#1a1a1a` 底 / `#242424` 卡片 / `#1e1e1e` 按压缩进），**主题色保持紫色**。
- 全局统一外边距 **16px 体系**：歌单页内容区、迷你歌词+播放栏底部包装、沉浸页均与窗口框架保持一致的左右留白。
- 深浅色切换入口：设置对话框 → `MainWindow._apply_theme()`；默认跟随系统（15s 定时器自动检测切换）。

### 2. 播放核心（app/player.py）
- 状态机：`list` 列表循环（默认）/ `random` 随机 / `single` 单曲循环 / `order` 顺序。
- `_next_index(manual)` 决定切歌逻辑；`_history` 支持随机模式"上一曲"。
- `set_value(v, notify)` 区分程序更新（不发射）与用户拖动（发射）→ 进度条不会与播放状态互扰。
- `volume_changed` 信号：音量变化（含静音/恢复）实时同步 UI 图标与滑块。
- `restore(song, position_ms)`：启动恢复——加载上次歌曲用于显示、不自动播放、恢复进度（`_maybe_restore` 在首次歌单加载完成后调用）。
- 播放模式、音量持久化到 `config.json`。

### 3. Subsonic / Jellyfin 客户端（app/subsonic_client.py）
- 认证：`token+salt`（`u/t/s/v/c/f` 参数），`ping` 用于连接验证。
- **键兼容**：音桥（yinqiao）返回原生 JSON（纯键 `id`/`name`/`title`/`coverArt`），标准 Subsonic XML→JSON 用 `@id`/`@name`。统一用 `_get(node, "id", "@id", ...)` 兼容两种风格，改解析器时务必沿用。
- 已实现 Subsonic：`getPlaylists`、`getPlaylist`、`getStarred2`、`star/unstar`、`getLyrics`、`getLyricsBySongId`（结构化时间轴歌词，音桥可用）、`getAlbumList2`、`getAlbum`、`getArtists`、`getArtist`、`search3`、`getPlayQueue`、`stream`、`getCoverArt`。
- **完整歌单（Jellyfin 补全）**：音桥 Subsonic `getPlaylist` 固定只返回前 50 首且忽略分页 → `get_playlist_songs()` 检测到"忽略分页"后改用 **Jellyfin 协议**分页拉取 `Playlists/fnm-playlist-{id}/Items`，再把条目映射回 Subsonic `Song`（剥离 `fnm-track-/fnm-album-/fnm-artist-` 前缀得到 Subsonic 侧 id，流地址沿用 Subsonic `stream`，封面走 Jellyfin 公开图片接口 `/Items/{fnm-id}/Images/Primary`）。`get_playlist_counts()` 用 `getPlaylist` 的 songCount 补充真实歌曲数。
- 数据类：`Song / Playlist / Album / Artist / LyricLine`；`Song` 含 `artist_id`/`album_id`（用于点击跳转）。
- `cover_url()` 按 `fnm-` 前缀路由到 Jellyfin 图片接口，否则走 Subsonic `getCoverArt`。
- JSON 响应包裹在 `subsonic-response` 中；子元素单值时是 dict，需 `_as_list()` 归一。
- 流/封面 URL 带认证参数，直接交给 QMediaPlayer / QNetworkAccessManager；音桥支持 HTTP Range（206），可拖动进度。

### 4. 页面与交互
- **连接页**：居中卡片 + 内凹输入框 → 后台 `PingThread` 验证 → 成功保存配置并进入歌单页。
- **歌单页**：侧边栏（导航 + 我的歌单列表）＋ 主内容区（QStackedWidget 四视图）＋ 迷你歌词条 ＋ 底部播放栏。
  - 视图：`_VIEW_SONG` 歌曲列表（通用 `SongHeader` + 可点击 `SortHeader` + 虚拟化 `QListView`）、`_VIEW_ALBUM` 专辑网格（IconMode，流体自适应）、`_VIEW_ARTIST` 歌手双栏（左窄列 + 右歌曲列表）、`_VIEW_PLACEHOLDER`。
  - 导航：首页=最近专辑、收藏(getStarred2)、最近播放(getPlayQueue)、歌手、专辑；「歌单」只在「我的歌单」栏（与导航**唯一选中互斥**），「设置」在标题栏最小化左侧。
  - **排序**：`SortHeader` 三列可点击循环（歌名/歌手 5 态、专辑/时长 3 态），`_apply_sort()` 稳定排序；切回"默认"恢复原始顺序（`_base_songs` 缓存）。
  - **歌曲行封面交互**：`SongDelegate` 悬浮变暗+实心三角/暂停（行悬浮非图标白色、悬停图标主题色）、点击播放变暂停、当前播放行四柱律动（白色、居中、`_anim_timer` 刷新）；命中检测在 `LibraryPage.eventFilter`（MouseMove/Click）。
  - **点击跳转**：歌手名/专辑名可点击（`song_zone` 收窄到文本实际宽度、悬浮高亮下划线），跳转歌手页/专辑页；播放栏歌手名也可点。
  - **歌手排序**：`get_artist_counts_fast()` 合并 3 种 getAlbumList2 各 500 专辑按歌手聚合歌曲数（秒级，避免逐歌手请求）。
  - **歌单加载**：`_playlist_cache` 缓存已加载歌单 + **两段式**：第一段 `get_playlist_songs(jellyfin=False)` 快取前 50 首立即可用，第二段后台拉全量（含 Jellyfin 补全）后无缝替换并写缓存；SongHeader 刷新按钮强制重载。
  - **自定义封面**：SongHeader 刷新按钮右侧编辑（铅笔）图标 → 菜单「选择本地图片… / 使用服务器封面 / 使用自定义封面」；图片复制到 `APP_DIR/covers/`，配置存 `player.playlist_covers`（`{id:{mode,path}}`）；`PlaylistDelegate._local_pixmap` 缓存本地 QPixmap，侧边栏行与头部大封面自定义优先。
  - **迷你歌词**：`MiniLyrics` 播放栏上方显示当前句（12.5pt），背景**底部半透明→顶部全透明**向上渐隐；侧边栏图层高于歌词层。
  - **播放栏**：左 = 封面 + [爱心与歌名同行（点亮变实心 `heart_filled`）/ 歌手与爱心左对齐]（MarqueeLabel 跑马灯）；控制组左右等宽真居中，上/下一曲用 `SkipButton`（实心三角+竖条）；音量 `VolumeControl` 常显 120px 滑块（点击图标静音/恢复记忆音量，调整/悬浮显示百分比）；进度条悬浮时左侧时间实时显示鼠标位置时间（`HoverSlider.hoverValueChanged/hoverEnded`）。
  - **侧边栏歌单**：`PlaylistDelegate` 绘制真实封面缩略图（120px 请求）+ 真实歌曲数（暗色无单位）；行状态区分**悬浮(press)/选中(primary_soft + 左侧主题色指示条)**。
- **沉浸页**（参考"沉浸页布局参考.png"）：点播放栏封面 → `_TransitionOverlay` 放大到沉浸页**新封面位置**（圆角在源/目标间插值；首次打开前先强制布局避免目标过大）；**左右 50%** 分栏：封面居左（随窗口自适应 `min(高度×0.42, 宽度×0.32, 360)`）、歌词居右（左对齐、当前行垂直居中+距离渐隐）；打开时封面由过渡层放大、**其它内容渐变出现**（`PlayerPage.fade_in`，QGraphicsOpacityEffect）；底部进度条（含悬浮预览）+ 控制栏（爱心在播放顺序键左侧，左下不显示歌曲信息）。
- 页面间通过共享的 `player` / `covers` / `lyrics` 信号联动。

### 5. 全局快捷键（app/hotkeys.py）
- **系统媒体键**：`MediaKeyHook`（WH_KEYBOARD_LL 低级钩子）全局捕获播放/暂停、上/下一曲、停止、音量±/静音，不依赖窗口焦点；`_HotkeyFilter` 同时处理 **WM_APPCOMMAND** 兜底（窗口聚焦时）。
- **自定义组合键**：`parse_combo("Ctrl+Alt+P")` → (modifiers, vk) 走 RegisterHotKey，默认在 `config.hotkeys`。
- 动作 `play_pause/next/prev/volume_up/volume_down` 在 `MainWindow._on_hotkey` 路由。

### 6. 配置（app/config.py）
- `config.json` 存于项目根目录（不入库）。字段：`server`、`player`（volume/mode/上次歌单/上次歌曲/上次进度/窗口尺寸/歌单自定义封面）、`theme`（`system`/`light`/`dark`，默认跟随系统）、`hotkeys`。
- **打包**：`config.py` 兼容 PyInstaller（`sys.frozen` 判断 → 持久化文件写 exe 目录、资源走 `_MEIPASS` 临时目录）。
- **重启恢复**：`MainWindow._on_connected` 连接后恢复音量与播放模式（构建播放栏前），`LibraryPage._maybe_restore` 默认打开上次歌单并显示上次歌曲。
- 深浅色解析：`theme.effective_dark()`（`system` 读注册表 `AppsUseLightTheme`）；跟随系统模式下 15s 定时器自动检测切换。

### 7. 无边框窗口（ui/windows.py）
- `_NativeFilter` 处理 WM_NCHITTEST（拖动标题栏、8px 边缘缩放、按钮区返回 HTCLIENT）与 WM_GETMINMAXINFO（最小尺寸）。
- 标题栏：品牌 + 设置按钮（最小化左侧）+ 最小化/最大化/关闭（**常显**，含沉浸页）；标题栏区域返回 HTCAPTION → **双击标题栏最大化/还原**（Windows 系统行为）。
- **托盘**：关闭窗口默认最小化到 `QSystemTrayIcon`（不退出），双击/右键菜单「显示主界面」恢复、「退出」才真正退出（`self._tray_menu` 持引用防 GC；`_quit_app` = `player.stop()` → `close()` → `QApplication.quit()` 显式结束进程）；托盘不可用时关闭直接退出。
- **窗口尺寸记忆**：启动从 `config.player.window_size` 恢复（默认 1080×720、带范围校验），`closeEvent` 保存。
- 沉浸页内容区上边距=左边距=12（标题栏常显，返回键紧贴其下）；激活时封面由过渡层放大、其它内容 `fade_in` 渐变出现。
- 应用图标 `resources/app_icon.ico`（main.py 与窗口设置）。

## 五、数据流速览

```
启动 → 有配置? → PingThread 自动重连 → 成功 → LibraryPage
                  │ 失败 → ConnectPage 填写
连接成功 → CoverManager.set_client / Player.set_client
歌单页点击歌单 → DataThread(get_playlist_songs) → SongModel → QListView
双击/播放全部 → player.set_queue(songs, idx) → stream URL → QMediaPlayer
position_changed → 进度条 + LyricsWidget.set_position（沉浸页）
点封面 → open_immersive(pix, rect) → TransitionOverlay → PlayerPage
```

## 六、测试方法

- 冒烟：`$env:MUP_SMOKE="1"; python main.py`（2 秒自动退出）。
- 页面级：用假客户端实例化各页面（参考历史 `_shots.py` 思路，可临时重建）。
- 真实联调：在连接页填写真实「音桥」地址/账号（如 `192.168.0.116:14040`）。
- 日志：`logs/mup.log`（Python 未捕获异常 + Qt 消息）。

## 七、已知限制与后续规划

1. 首页/专辑/歌手/最近播放已实现；搜索入口已从侧边栏移除（客户端 `search3` 仍可用）。
2. 快捷键无图形化编辑界面，暂在 `config.json` 修改。
3. 封面仅内存缓存；歌词支持结构化时间轴 + LRC/纯文本回退；未实现音质选择、均衡器。
4. 未配置 PyInstaller 打包。
5. 音桥限制（已绕过的 / 仍存在）：Subsonic `getPlaylist` 硬限 50 首已通过 **Jellyfin 协议补全**解决；`getAlbumList2` 单次上限 500 张、`getArtists` 不含 albumCount。歌手"歌曲数"采用 3 种专辑列表聚合的近似值（秒级），非精确全量统计。完整歌单依赖音桥暴露的 Jellyfin 协议，普通 Subsonic 服务器不受影响。

---

## 八、文档与版本管理约定（重要）

> 对以上三个文件（`更新日志.md`、`项目细节描述.md`、`README.md`）都会在后续修改中更新，并正确使用 git 进行版本管理。
> 同时每次对话更新 `提示词记录.txt`（记录提示词输入与版本号变化）。后续 AI 修改代码后，必须同步更新这四个文件并提交 git。
